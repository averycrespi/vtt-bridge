{
  "name": "vtt-bridge",
  "description": "Connect Dungeon Master's Vault to Roll20.",
  "author": "Avery Crespi",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.1",
    "eslint-plugin-prettier": "^3.1.2",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.0.2",
    "web-ext": "^4.2.0"
  },
  "scripts": {
    "build": "parcel build --no-source-maps src/scripts/* -d dist",
    "clean": "rm -r dist/*",
    "lint": "web-ext lint",
    "start": "web-ext run",
    "watch": "parcel watch --no-source-maps src/scripts/* -d dist"
  }
}
